# Credentials
config.env

# Python
__pycache__/
*.pyc
*.pyo

# PyInstaller
build/
dist/
*.spec.bak
# Build output. Released as a zip on GitHub Releases by
# .github/workflows/release.yml, not committed — see the README.
/Jeeves.exe
/_internal/

# Runtime data
*.lock
*.db
rank_links.json
mod_update_log.json
logs/
# Lua bridge files live in the server's Zomboid folder. A stray ./Lua appears
# when the bot runs somewhere the Windows SERVER_INI_PATH can't be resolved
# (e.g. under WSL), where it collapses to a relative path.
Lua/

# IDE
.vscode/
.idea/
